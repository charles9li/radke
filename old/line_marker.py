import itertools

# List of lines
lines = itertools.cycle(("-", "--", ":", "-."))

# List of markers
markers = itertools.cycle(('o', 'v', 's', '8', 'p', '^', '<', '>'))

# List of colors
colors = itertools.cycle(('b', 'g', 'r', 'c', 'm', 'y', 'k', 'w'))
